{
  "scripts": {
    "build": "webpack --no-watch",
    "watch": "webpack",
    "start:firefox": "web-ext run --source-dir=dist",
    "dev": "(trap 'kill 0' SIGINT; yarn watch & yarn start:firefox & wait)"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^11.0.0",
    "watch": "^1.0.2",
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4"
  }
}
